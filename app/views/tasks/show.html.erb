<p style="color: green"><%= notice %></p>

<%= render @task %>

<div>
  <%= link_to "Edit this task", "/tasks/#{@task.id}/edit" %>
  <%= link_to "Back to tasks", "/tasks" %>
  <%= button_to "Destroy this task", "/tasks/#{@task.id}", method: :delete %>
</div>
